# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Pylance Release Timer

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

permissions:
  contents: read
  actions: write

concurrency:
  group: pylance-release-timer
  cancel-in-progress: false

jobs:
  trigger-update:
    runs-on: ubuntu-latest
    # Only run in the upstream repo; forks lack LANCE_RELEASE_TOKEN and would
    # fail this job on every scheduled tick.
    if: github.repository == 'lance-format/lance-ray'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check for new Pylance tag
        id: check
        env:
          GH_TOKEN: ${{ secrets.LANCE_RELEASE_TOKEN }}
        run: |
          python3 ci/check_pylance_release.py --github-output "$GITHUB_OUTPUT"

      - name: Look for existing PR
        if: steps.check.outputs.needs_update == 'true'
        id: pr
        env:
          GH_TOKEN: ${{ secrets.LANCE_RELEASE_TOKEN }}
          LATEST_PYPI_VERSION: ${{ steps.check.outputs.latest_pypi_version }}
          UPDATE_BRANCH: ${{ steps.check.outputs.update_branch_name }}
        run: |
          set -euo pipefail
          TITLE="chore: update pylance dependency to v${LATEST_PYPI_VERSION}"
          # Match by branch head (deterministic) OR by title (PEP 440 format,
          # produced by codex-update-pylance-dependency.yml). Search both open
          # and closed states so we don't reopen a manually-closed duplicate.
          BRANCH_COUNT=$(gh pr list --head "$UPDATE_BRANCH" --state all --limit 1 --json number --jq 'length')
          TITLE_COUNT=$(gh pr list --search "\"$TITLE\" in:title" --state all --limit 1 --json number --jq 'length')
          if [ "$BRANCH_COUNT" -gt 0 ] || [ "$TITLE_COUNT" -gt 0 ]; then
            echo "Existing PR found for branch=$UPDATE_BRANCH or title=$TITLE"
            echo "pr_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "No existing PR for branch=$UPDATE_BRANCH or title=$TITLE"
            echo "pr_exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Trigger codex update workflow
        if: steps.check.outputs.needs_update == 'true' && steps.pr.outputs.pr_exists != 'true'
        env:
          GH_TOKEN: ${{ secrets.LANCE_RELEASE_TOKEN }}
        run: |
          set -euo pipefail
          TAG=${{ steps.check.outputs.latest_tag }}
          gh workflow run codex-update-pylance-dependency.yml -f tag=refs/tags/$TAG

      - name: Show latest codex workflow run
        if: steps.check.outputs.needs_update == 'true' && steps.pr.outputs.pr_exists != 'true'
        env:
          GH_TOKEN: ${{ secrets.LANCE_RELEASE_TOKEN }}
        run: |
          set -euo pipefail
          gh run list --workflow codex-update-pylance-dependency.yml --limit 1 --json databaseId,url,displayTitle
